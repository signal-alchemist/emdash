---
"emdash": minor
---

Adds a read-only GitHub content-sync planning route that fetches a verified merge commit, validates bounded content and media catalogs, and returns a deterministic plan with rename and removal quarantine actions without mutating EmDash content, media, or storage.
