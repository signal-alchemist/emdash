---
"emdash": minor
---

Adds revision-aware content update, rename, publish, and unpublish operations to the plugin context. Existing plugin writes remain backward-compatible when no expected revision is supplied. Plugin media reads and uploads now expose authoritative SHA-256 metadata and lookup support, with a forward-only media migration.
