---
"emdash": minor
---

Adds a host-side signed GitHub webhook ingress for `sa-github-content-sync`. Configure exact repository and branch policies plus an environment-variable name for the secret; accepted merged pull-request deliveries are normalized before the sandbox plugin receives them.
